# 合并前检查记录（持续更新至 2026-09-04）

## 检查范围

本次针对当前分支进行合并前检查，重点关注学生端真实学习链路、后端数据一致性、权限边界和演示数据兜底。推荐结果不自动驱动关卡跳转属于当前设计取舍，本次不作为缺陷处理。

## 当前合并结论

当前分支暂不具备直接合并主分支的条件。自动评价、学习证据、知识掌握、成长投影、徽章和双能力向量已经完成真实联调，但仍有以下合并阻断项：

- 前端多个页面在身份或课程缺失时仍使用学生 1、课程 1 作为兜底，存在串看数据风险；
- 数据中心统计接口与“当前课程”展示口径不一致；
- 当前数据库只有课程 1，联调数据直接写入正式课程，尚未完成测试数据隔离；
- 当前工作区包含大量未提交修改和无关文件，尚未形成可审阅的合并提交。

包体积警告、死代码和图表资源释放问题列为 P2，不阻断本轮核心合并，但应进入后续迭代。

## 合并前分阶段处理计划

以下阶段按顺序执行。每一阶段都必须完成“实现 → 定向测试 → 全量回归 → 真实接口验证 → 文档更新”，当前阶段未通过不得进入下一阶段。

### 阶段一：身份、权限和课程上下文收口

目标：任何业务页面都不能在身份或课程缺失时显示固定学生、固定课程或其他用户的数据。

当前状态：实现完成，环境冒烟待补。已完成统一上下文工具、主要页面固定身份/课程兜底移除、登录与角色路由元数据、课程缺失时跳转课程选择页及前端回归测试；真实未登录/越权浏览器冒烟因当前浏览器连接连续超时未能完成，不能据此判定通过。

本轮已验证：前端测试 184 项通过，生产构建通过；固定学生号/课程号的核心请求兜底已移除，未登录访问受保护路由的自动化守卫用例已补充。

阶段一实现收尾记录：新增身份上下文测试、路由角色测试和课程缺失测试共 8 项；全局扫描未发现身份请求继续默认到学生 1 或课程 1 的主要路径。浏览器冒烟尚未形成有效证据，进入阶段五前必须在独立未登录会话中补测。

任务：

1. 删除 `FloorView.vue`、`TowerMap.vue`、`DataCenter.vue`、`ProgressView.vue`、`StudentProfile.vue` 等页面中的固定 ID 兜底；
2. 统一从登录态和明确的课程上下文获取学生号、教师号和课程号；
3. 未登录跳转登录页，缺少课程跳转课程选择页，身份不匹配时显示权限错误；
4. 补齐路由登录守卫和学生/教师/管理员角色守卫；
5. 检查所有画像、统计、能力、任务和成长接口的课程归属校验。

预计输出：统一身份/课程上下文工具、路由守卫、页面错误状态、对应前端测试和越权接口测试。

验收标准：

- 未登录访问 `/tower-map`、`/data-center`、`/profile`、`/progress` 时全部进入登录流程；
- 删除 localStorage 中的学生号或课程号后，页面不再请求学生 1 或课程 1；
- 学生 A 访问学生 B 的画像、成长和能力接口均返回无权限；
- 教师只能访问自己授课课程的数据；
- 不允许出现固定 ID 作为业务请求参数的生产代码路径。

### 阶段二：能力映射数据库和版本一致性

目标：能力映射数据在数据库层面具备完整约束，正式矩阵不会被孤儿数据、异常版本或旧候选污染。

当前状态：已完成。已补齐映射关系、观测任务和矩阵版本的外键与 CHECK 约束；迁移启动前检查并拒绝孤儿引用，不再静默删除；生产数据库增加每课程唯一正式版本索引；正式版本查询仅对“无记录”回退 `v1`，数据库异常直接抛出。候选版本仍以课程行锁和 `based_on_version` 做发布前校验。

本轮验证：新增映射服务异常传播测试；后端全量测试 385 项通过、0 失败、0 错误、5 项跳过。H2 测试环境对不支持的条件索引采用等价的启动前重复版本检查，Kingbase 生产迁移使用条件唯一索引。

任务：

1. 为关系、观测任务和矩阵版本补充课程、能力点、真能力和任务外键；
2. 增加关系状态、版本状态、强度、置信度和样本数的 CHECK 约束；
3. 处理删除真能力时的关系和观测任务归档/级联策略；
4. 补充每课程唯一正式版本的数据库约束或等价事务保证；
5. 修复 `publishedVersion`：只有明确无记录时才返回默认版本，数据库异常必须向上抛出并记录；
6. 保留候选版本基线校验，验证旧候选不能覆盖新正式版本。

预计输出：可重复执行的 Kingbase 迁移、约束核验脚本、版本并发测试、异常传播测试。

验收标准：

- 伪造课程、能力点、真能力或任务编号的写入全部被数据库或服务端拒绝；
- strength、confidence、状态值越界写入失败；
- 每个课程最多一个 published 版本；
- 正式版本更新后，旧基线候选发布返回冲突，正式版本不变；
- 模拟数据库连接异常时不会返回 `v1` 并继续业务流程；
- 在干净数据库执行迁移两次均成功，且无孤儿记录。

### 阶段三：课程级统计和真实数据隔离

目标：统计、成长和能力数据只来自当前课程，正式课程与联调数据完全隔离。

当前状态：核心实现和真实数据隔离验收完成；完整自动评价闭环仍待后续阶段验收。已完成数据中心改用课程级学生统计接口、后端课程统计补充明细和课程学习时长、移除查询异常时的跨课程回退，并为演示数据脚本增加显式 `SEED_ALLOW_DEMO=1` 安全开关。后端统计定向测试、后端全量回归和前端 184 项测试均通过，前端生产构建通过。

本轮发现并修复：数据中心此前调用无课程条件的 `/api/students/{studentNo}/stats`，导致页面可能展示学生其他课程的提交；现在统一调用 `/api/student/{studentNo}/course/{courseCode}`。课程提交查询异常时不再回退读取该学生全部课程的提交。

真实 Kingbase 验收记录（2026-09-04）：创建隔离课程 `1000`、任务 `1005`，分配给测试学生 `2` 并提交 1 次答案。课程 `1000` 统计返回 `totalSubmissions=1`、`completedCount=1`、`totalTasks=1`；课程 `1` 统计不包含任务 `1005`。该提交当前状态为“待教师评阅”，因此自动评价结果不作为本阶段已通过项。

任务：

1. 将数据中心改为调用课程级学生统计接口；
2. 所有统计卡片、提交记录、成长记录和能力变化使用同一课程上下文；
3. 创建独立测试课程和测试账号，迁移或隔离现有自动评价联调任务；
4. 明确开发种子数据与生产初始化数据，生产启动不得自动写入演示记录；
5. 在干净数据库验证迁移、初始化和课程开关状态。

预计输出：课程级统计接口契约、独立测试课程、测试数据清单、干净库验收记录。

验收标准：

- 当前课程统计不会读取其他课程的提交或成绩；
- 测试提交不会出现在正式课程的任务、画像和成长列表中；
- 干净数据库仅执行生产初始化时，不产生联调账号和测试提交；
- 测试课程可独立完成“提交 → 自动评价 → 证据 → 掌握度 → 成长/徽章/能力”的闭环。

### 阶段四：能力映射前端完整性和错误处理

目标：教师修改能力映射时操作完整、失败可恢复，不出现部分成功或旧课程残留。

当前状态：实现和自动化验收完成，浏览器现场验收待最终阶段补做。已补齐真能力编辑/删除、课程切换清理、映射加载错误状态，以及观测任务事务性批量保存；前端不再用多个并行请求造成观测标注部分成功。后端全量测试 386 项通过、0 失败、0 错误、5 项跳过；前端 184 项通过，生产构建通过。

本轮新增接口：`PUT /api/ability-competency-map/observations/batch`。同一任务的全部真能力标注由一次事务提交，任一项校验失败会整体回滚。

任务：

1. 补齐真能力编辑、保存和删除入口；
2. 将观测任务保存改为后端批量原子接口，或提供逐项结果和补偿机制；
3. 课程切换时清空旧数据，分别处理映射、候选、报告和观测接口失败；
4. 增加候选版本、校准报告和草稿状态的重新加载接口；
5. 统一处理未登录、无权限、接口失败、空数据和真实暂无记录。

预计输出：完整教师端能力映射流程、批量保存接口、失败重试和组件测试。

验收标准：

- 编辑已有真能力后刷新页面仍保持修改结果；
- 批量保存任一项失败时数据库不产生部分成功；
- 切换课程后页面不显示上一课程的能力、候选或报告；
- 接口 401、403、500 和空数组分别显示不同状态，并提供重试入口。

### 阶段五：最终回归和合并卫生

目标：形成可审阅、可复现、可回滚的主分支合并提交。

当前状态：测试回归和合并卫生完成，浏览器现场验收仍未通过。Java 17 后端全量测试 387 项通过、0 失败、0 错误、5 项跳过，JaCoCo 报告已生成；前端 184 项测试通过，生产构建通过，`git diff --check` 通过。真实 Kingbase HTTP 链路已核验；浏览器控制插件已成功连接并打开登录页，但进入数据中心时导航超时，尚未形成完整现场验收证据。当前工作区已整理为检查点提交，无 `resume_*` 无关产物。

最终阻断项：

1. 需要在浏览器页面加载稳定后完成未登录、课程切换、能力映射编辑和学生数据展示的现场验收；
2. 需要决定是否保留真实 Kingbase 验收课程 `1000`、任务 `1005` 和提交记录，或由维护人员按数据清单清理。

任务：

1. 在 Java 17 环境执行后端全量测试和 JaCoCo；
2. 执行前端全量测试、生产构建和 `git diff --check`；
3. 在干净数据库完成迁移和真实浏览器链路；
4. 清理测试课程之外的临时数据、日志和无关产物；
5. 按功能拆分提交，排除 `resume_*` 等无关文件；
6. 更新本文件和代码审查问题记录，确认没有新增 P0/P1。

预计输出：最终测试报告、数据库迁移报告、浏览器验收记录、干净提交和合并清单。

验收标准：

- 后端测试 0 失败、0 错误，JaCoCo 插桩和报告正常；
- 前端测试和生产构建通过；
- 真实链路关键请求全部进入真实后端，不使用 Mock 或本地启发式结果；
- 工作区只包含本次功能相关变更；
- 所有 P0/P1 问题关闭后，才允许合并主分支。

## 已处理

### 删除后端虚假关卡兜底

`TowerRunServiceImpl` 在课程没有知识点时，原先会生成 `demo-kp / 课程基础挑战`，导致课程数据缺失时仍显示可玩的虚假路线。

现已改为：

- 没有知识点时不生成虚假节点；
- 返回“课程尚未配置知识点，无法生成学习路线”；
- 前端显示明确错误状态。

## 检查中发现的问题及处理状态

### 1. ProfileApiController 的学生身份边界不够严格

原先 `/api/students/{studentId}/**` 部分接口主要判断是否登录，没有统一校验 `studentId` 是否属于当前学生。

现已处理：

- 学生只能访问和操作自己的 `studentId`；
- 管理员保留运维访问能力；
- 画像、爬塔、能力、成长值、成就、推荐和题包接口统一使用本人校验；
- `game-event` 已补充会话校验，不能再伪造其他学生事件。

### 2. 登录响应携带 Student 实体

已处理：

- 新增非实体的 `LoginResponse` DTO；
- 学生和教师登录接口均改为返回脱敏 DTO；
- Session 仍保存完整实体，现有认证和业务逻辑不受影响；
- 登录响应不再包含密码字段。

## 验证结果

- 后端测试：Java 17 下 387 个通过，0 个失败，0 个错误，5 个真实联调测试跳过；JaCoCo 报告已生成。
- 前端测试：184 个通过。
- 前端生产构建：通过。
- `git diff --check`：通过。
- 后端通过 SSH 隧道连接小型服务器 Kingbase：启动成功。
- 前端固定地图、虚构宝箱奖励和本地战斗结算兜底已移除。
- 学生本人画像访问实测通过，访问其他学生画像返回“无权限”。

### 3. 数据中心深层路由导航复测（2026-09-04 19:20）

本轮复测确认，之前记录的“数据中心导航超时”包含真实前端部署问题，但不是统计接口性能问题：

- 前端服务未启动时，`3000` 端口不可用；启动开发服务后因端口占用自动使用 `3002`。
- 在修复前，直接访问 `/login` 和 `/data-center` 均返回 `404 Cannot GET`，说明 HTML5 history 路由没有明确配置服务端回退。
- 相关后端请求在同一运行环境下均快速返回：画像摘要约 15ms，无会话时正确返回权限错误；能力图谱/掌握度/真能力/雷达接口约 16–550ms，无发现持续阻塞请求。
- 已在 `frontend/vue.config.js` 明确配置 `historyApiFallback`，修复后 `/login`、`/data-center` 均返回前端入口 `200`。
- 前端 184 项测试通过，生产构建通过。浏览器能稳定打开登录页；完整登录后的数据中心现场验收仍需有效会话，因此暂不将其标记为最终通过。
- 19:22 再次验收：`/login` 与 `/data-center` 均返回 `200`，课程统计接口约 8ms、能力图谱接口约 1.5ms；浏览器标签页已到达 `/data-center` 且无控制台错误，但浏览器控制层仍未能完成该页的状态读取。该超时没有得到后端慢请求证据，暂记为浏览器现场工具限制/未登录现场验收，不重新归因于业务接口。
- 19:30 完成登录现场复测：修复登录页忽略 `redirect` 的问题后，使用测试学生登录并回跳到 `/data-center?courseId=1000`，页面稳定显示真实提交数、完成任务数、平均得分、学习状态和最近记录；能力图谱区域按真实空数据展示“暂无能力图谱”，控制台无错误。前端测试 184/184 通过，生产构建通过。

结论：该问题应保留为前端服务配置问题并已修复；剩余现场验收项是有效登录会话下的数据中心渲染确认，不应继续按“后端统计接口超时”追查。后端启动必须使用项目兼容的 Java 17 和 Kingbase 本地配置，Java 25 启动会因驱动未加载而失败。
