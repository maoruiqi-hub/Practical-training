# 模块接口与协作规范

> **文档定位**：本文档是5个模块之间的**接口契约**——每个模块"对外暴露什么，需要他人提供什么"。
> 在 `specs/项目需求.md`（应该建什么）确定后，本文档定义模块间**怎么配合**，是每个人独立开发的上手指南。

---

## 一、架构总览

```
┌──────────────────────────────────────────────────────────────┐
│  Frontend                                                     │
│  教师端 · 学生端                                              │
└──────────────────────────┬───────────────────────────────────┘
                           │ RESTful API（统一网关）
                           │
┌──────────────────────────┴───────────────────────────────────┐
│  Backend（单体后端，模块化包结构）                              │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────┐│
│  │ 模块1    │ │ 模块2    │ │ 模块3    │ │ 模块4    │ │模块5 ││
│  │ 课程内容  │ │ 学习任务  │ │ 测评评价  │ │ 学生画像  │ │学情  ││
│  │ 知识组织  │ │ 过程跟踪  │ │ 成果评价  │ │ 个性化    │ │分析  ││
│  └─────┬────┘ └─────┬────┘ └─────┬────┘ └─────┬────┘ └───┬──┘│
│        └─────────────┴────────────┴────────────┴──────────┘  │
│                              │                                │
└──────────────────────────────┼────────────────────────────────┘
                               │
┌──────────────────────────────┴───────────────────────────────────┐
│  agentic/（独立 AI 服务，非第6模块）                                │
│  知识图谱生成 · 智能批改 · 学习推荐 · 风险预警 · 问题聚类 · AI对话   │
└──────────────────────────────────────────────────────────────────┘
```

**关键约束**：

| 约束 | 说明 |
|------|------|
| 后端架构 | 单体应用 + 模块化包结构（每个模块一个 package），非微服务 |
| 模块间通信 | 同一进程内的方法/函数调用，不是 HTTP |
| AI 调用 | 所有 LLM 调用统一走 `agentic/` 服务，各模块只负责组装 prompt 和消费结果 |
| 数据归属 | 每个数据表只有一个模块负责写入，其他模块只读或通过接口查询 |

---

## 二、5 个模块定义

| 模块 | 核心职责 | 核心实体（谁拥有这张表） | 开发负责人 |
|------|---------|----------------------|-----------|
| 1. 课程内容与知识组织 | 课程教什么，知识点之间什么关系 | Course, Resource, KnowledgePoint, KnowledgeRelation | 待定 |
| 2. 学习任务与过程跟踪 | 学生做什么任务，留下哪些学习数据 | Task, Submission, LearningLog | 待定 |
| 3. 测评与成果评价 | 如何出题考试，如何评价提交成果 | Question, Exam, Answer, GradeResult | 待定 |
| 4. 学生画像与个性化学习 | 系统如何理解学生并给予适配支持 | Student, CompetencyScore, Recommendation, Achievement | 待定 |
| 5. 学情分析与教学决策 | 教师如何根据数据调整教学 | Class, AnalyticsReport, RiskAlert, TeachingSuggestion | 待定 |

---

## 三、共享标识符（Shared ID）规范

所有模块间通过 ID 引用对方的数据，不直接访问对方的数据表。

```
  模块1 产生             模块2 产生             模块3 产生             模块4 产生
  ─────────             ─────────             ─────────             ─────────
  CourseID              TaskID                QuestionID            StudentID
  ResourceID            SubmissionID          ExamID
  KnowledgePointID      LearningLogID         AnswerID
  KnowledgeRelationID                          GradeResultID
  AbilityPointID

  模块5 产生
  ─────────
  ClassID
  RiskAlertID
  ClusterID
  AnalysisReportID
```

**ID 格式约定**：所有 ID 使用 UUID v4 字符串（如 `"a1b2c3d4-..."`），由各模块生成，全局唯一。

---

## 四、模块1 详细接口：课程内容与知识组织

### 4.1 核心实体

| 实体 | 说明 | 关键字段 |
|------|------|---------|
| `Course` | 课程（当前 = Python编程与数据分析基础） | id, name, description, credits, hours |
| `Resource` | 课程资源（PPT/PDF/视频等） | id, course_id, type, title, file_url |
| `KnowledgePoint` | 知识点节点（塔的一层） | id, course_id, name, description, level, parent_type |
| `KnowledgeRelation` | 知识点关系（前置依赖等） | id, from_kp_id, to_kp_id, relation_type |
| `AbilityPoint` | 能力点（课程能力图谱节点） | id, course_id, name, description |

### 4.2 对外暴露接口

```
模块1 对外暴露（其他模块可以调用）：
──────────────────────────────────

# -- 课程 --
GET    /api/courses/{id}               → 返回课程基本信息
GET    /api/courses/{id}/structure     → 返回课程完整知识结构（知识点树）

# -- 知识点 --
GET    /api/knowledge-points/{id}      → 返回知识点详情
GET    /api/knowledge-points           → 按课程查询知识点列表
         ?course_id={id}
GET    /api/knowledge-graph            → 返回知识图谱（节点+边）
         ?course_id={id}
GET    /api/knowledge-points/{id}/prerequisites
                                       → 返回某知识点的所有前置知识点
GET    /api/ability-map                → 返回课程能力图谱
         ?course_id={id}

# -- 高频薄弱（供模块5消费） --
GET    /api/courses/{id}/weak-points   → 返回高频薄弱知识点排名
```

### 4.3 自身完整接口（含增删改）

```
# -- 课程（教师端） --
POST   /api/courses                    → 创建课程
PUT    /api/courses/{id}               → 修改课程

# -- 资源（教师端） --
POST   /api/courses/{id}/resources     → 上传资源
GET    /api/resources/{id}/preview     → 资源在线预览
DELETE /api/resources/{id}             → 删除资源

# -- 知识点（教师端） --
POST   /api/courses/{id}/knowledge-points/extract
                                       → 自动提取知识点（→ agentic）
POST   /api/knowledge-points           → 手动创建知识点
PUT    /api/knowledge-points/{id}      → 修改知识点
DELETE /api/knowledge-points/{id}      → 删除知识点
POST   /api/knowledge-relations        → 创建知识点关系
DELETE /api/knowledge-relations/{id}   → 删除知识点关系

# -- 智能讲解/问答（学生端） --
POST   /api/knowledge-points/{id}/explain
                                       → PPT知识点智能讲解（→ agentic）
POST   /api/knowledge-points/{id}/qa   → 知识点问答（→ agentic）

# -- 能力图谱（教师端） --
POST   /api/ability-map/generate       → 生成课程能力图谱（→ agentic）
PUT    /api/ability-points/{id}        → 编辑能力点
```

### 4.4 对外提供的数据查询方法（模块间调用）

```python
# 模块1 在代码中提供这些查询方法，供其他模块直接调用（非 HTTP）：

def get_course_info(course_id: str) -> CourseInfo
    """模块2/3/4/5 都需要知道课程基本信息"""

def get_knowledge_points_by_course(course_id: str) -> list[KnowledgePoint]
    """模块3 关联试题到知识点时需要"""

def get_prerequisite_chain(kp_id: str) -> list[KnowledgePoint]
    """模块4 计算前置知识掌握度（DEF）时需要"""

def get_ability_map(course_id: str) -> list[AbilityPoint]
    """模块4 更新能力评分时需要"""
```

### 4.5 依赖

| 依赖方 | 需要什么 | 数据从哪里来 |
|--------|---------|-------------|
| agentic | 课程大纲/教材文本 → 输出知识点列表+依赖关系 | LLM |
| 无模块间依赖（模块1是上游起点） | — | — |

---

## 五、模块2 详细接口：学习任务与过程跟踪

### 5.1 核心实体

| 实体 | 说明 | 关键字段 |
|------|------|---------|
| `Task` | 学习任务 | id, course_id, knowledge_point_id（可选）, type, title, description, deadline, submit_type, created_by |
| `Submission` | 学生提交 | id, task_id, student_id, content, files, submit_time, status |
| `LearningLog` | 学习行为日志 | id, student_id, task_id, resource_id, action_type, duration, detail, timestamp |

### 5.2 对外暴露接口

```
模块2 对外暴露：
────────────────

# -- 任务查询 --
GET    /api/tasks                       → 任务列表
         ?course_id={id}&student_id={id}
GET    /api/tasks/{id}                  → 任务详情

# -- 提交查询（供模块3打分、模块5查看） --
GET    /api/tasks/{id}/submissions      → 某任务的所有提交
GET    /api/students/{id}/submissions   → 某学生的所有提交
         ?course_id={id}

# -- 学习日志（供模块4计算画像、模块5分析） --
GET    /api/learning-logs               → 查询学习行为日志
         ?student_id={id}
         &course_id={id}
         &action_type={type}            # video_play, ppt_view, answer, code_submit...
         &start_time={}&end_time={}

# -- 进度统计 --
GET    /api/students/{id}/progress      → 学生整体学习进度
         ?course_id={id}
GET    /api/tasks/{id}/stats            → 任务完成统计
```

### 5.3 自身完整接口

```
# -- 任务管理（教师端） --
POST   /api/tasks                       → 发布任务
PUT    /api/tasks/{id}                  → 修改任务
DELETE /api/tasks/{id}                  → 删除任务

# -- 任务提交（学生端） --
POST   /api/tasks/{id}/submit           → 学生提交任务
PUT    /api/submissions/{id}            → 修改提交（截止前）

# -- 行为日志（后端自动采集，前端调用） --
POST   /api/learning-logs               → 记录一条学习行为
         body: {
           student_id, task_id, resource_id, knowledge_point_id,
           action_type, duration_ms, detail_json
         }
```

### 5.4 对外提供的数据查询方法

```python
def get_student_submissions(student_id: str, course_id: str) -> list[Submission]
    """模块3 批改时查询，模块5 分析完成情况时查询"""

def get_student_logs(student_id: str, course_id: str,
                     action_types: list[str], time_range: tuple) -> list[LearningLog]
    """模块4 计算画像时按类型和时间范围拉取日志"""

def get_task_completion_stats(course_id: str) -> dict
    """模块5 查看全局任务完成情况"""

def get_student_progress(student_id: str, course_id: str) -> StudentProgress
    """模块4/5 获取学生学习进度快照"""
```

### 5.5 依赖

| 依赖方 | 需要什么 | 说明 |
|--------|---------|------|
| 模块1 | CourseID, KnowledgePointID | 任务需要关联课程和知识点 |
| 模块4 | StudentID | 学生身份由模块4管理 |

---

## 六、模块3 详细接口：测评与成果评价

### 6.1 核心实体

| 实体 | 说明 | 关键字段 |
|------|------|---------|
| `Question` | 试题 | id, course_id, knowledge_point_id（关联模块1）, type, content, answer, difficulty, options |
| `Exam` | 试卷 | id, course_id, title, question_ids[], generate_type, total_score, time_limit |
| `Answer` | 学生作答 | id, exam_id, question_id, student_id, answer_content, is_correct, score |
| `GradeResult` | 评价结果 | id, target_id（submission或answer）, target_type, score, feedback, graded_by（auto/manual/ai） |

### 6.2 对外暴露接口

```
模块3 对外暴露：
────────────────

# -- 试题查询 --
GET    /api/questions                   → 题库列表
         ?course_id={id}&knowledge_point_id={id}&difficulty={level}

# -- 试卷查询 --
GET    /api/exams/{id}                  → 试卷详情（含试题列表）
GET    /api/exams                       → 试卷列表
         ?course_id={id}

# -- 成绩查询（供模块4画像、模块5分析） --
GET    /api/exams/{id}/scores           → 某次考试的全体成绩
GET    /api/students/{id}/scores        → 某学生的所有成绩
         ?course_id={id}

# -- 错题分析（供模块4推荐、模块5共性问题） --
GET    /api/students/{id}/mistakes      → 学生错题列表
         ?course_id={id}&knowledge_point_id={id}
GET    /api/courses/{id}/mistake-stats  → 课程错题统计（按知识点/题型分组）
```

### 6.3 自身完整接口

```
# -- 题库管理（教师端） --
POST   /api/questions                   → 创建试题
PUT    /api/questions/{id}              → 修改试题
DELETE /api/questions/{id}              → 删除试题
POST   /api/questions/import            → 批量导入试题
POST   /api/questions/{id}/link-kp      → 关联知识点

# -- 组卷（教师端） --
POST   /api/exams/generate              → 智能组卷
         body: {
           course_id, question_count, difficulty_mix,
           knowledge_point_ids[], generate_type  # random / by_kp / by_difficulty
         }

# -- 答题（学生端） --
POST   /api/exams/{id}/submit           → 提交试卷答案
         body: { answers: [{question_id, answer_content}, ...] }

# -- 智能批改（→ agentic） --
POST   /api/submissions/{id}/grade      → 对学生提交进行AI评价
         body: { submission_type: "report"|"essay"|"short_answer" }
         # 返回: GradeResult { score, feedback, suggestions }

# -- 重批 --
PUT    /api/grade-results/{id}          → 教师手动调整分数/反馈
```

### 6.4 对外提供的数据查询方法

```python
def get_student_scores(student_id: str, course_id: str) -> list[ScoreRecord]
    """模块4 计算能力评分时需要，模块5 成绩报表时需要"""

def get_student_mistakes(student_id: str, course_id: str) -> list[MistakeRecord]
    """模块4 推荐错题相关练习时需要"""

def get_class_mistake_stats(course_id: str) -> ClassMistakeStats
    """模块5 共性问题聚类时需要"""

def get_exam_scores(exam_id: str) -> list[ScoreRecord]
    """模块5 某次考试的成绩分布"""
```

### 6.5 依赖

| 依赖方 | 需要什么 | 说明 |
|--------|---------|------|
| 模块1 | KnowledgePointID | 试题必须关联知识点 |
| 模块2 | SubmissionID | 评价的是学生提交的成果 |
| 模块4 | StudentID | 成绩归属学生 |
| agentic | 学生提交内容 + 评分标准 | 智能批改需要 LLM 评分 |

---

## 七、模块4 详细接口：学生画像与个性化学习

### 7.1 核心实体

| 实体 | 说明 | 关键字段 |
|------|------|---------|
| `Student` | 学生信息 | id（= auth user id）, name, class_id, email, enrollment_year |
| `StudentProfile` | 学习画像 | student_id, course_id, hp, atk, def, exp, level, coins, energy |
| `CompetencyScore` | 能力评分 | student_id, course_id, ability_point_id, score, last_updated |
| `Recommendation` | 推荐记录 | student_id, course_id, type, target_id, reason, priority, created_at |
| `Achievement` | 成就/徽章 | student_id, achievement_type, earned_at, metadata |

### 7.2 对外暴露接口

```
模块4 对外暴露：
────────────────

# -- 学生信息 --
GET    /api/students/{id}               → 学生基本信息
GET    /api/students                    → 学生列表
         ?class_id={id}

# -- 画像数据（供模块5预警、模块2任务适配） --
GET    /api/students/{id}/profile       → 完整学生画像（HP/ATK/DEF/EXP/等级）
         ?course_id={id}

# -- 能力评分（供模块1能力图谱展示、模块5分析） --
GET    /api/students/{id}/competency    → 学生能力评分明细
         ?course_id={id}

# -- 推荐记录（供前端展示） --
GET    /api/students/{id}/recommendations
                                       → 当前推荐列表
```

### 7.3 自身完整接口

```
# -- 学生信息管理（管理员/教师） --
POST   /api/students                    → 录入学生
PUT    /api/students/{id}               → 修改学生信息
POST   /api/students/import             → 批量导入
GET    /api/students/export             → 批量导出

# -- 画像生成（系统自动，也可手动触发） --
POST   /api/students/{id}/profile/generate
                                       → 生成/刷新学习画像（→ agentic）

# -- 能力评分更新（系统自动） --
POST   /api/students/{id}/competency/update
                                       → 根据最新学习数据更新能力评分

# -- 个性化推荐（→ agentic） --
POST   /api/students/{id}/recommendations/generate
                                       → 生成新一批推荐
         body: {
           course_id,
           recommendation_types: ["knowledge_point", "review_material", "extended_material", "practice"]
         }

# -- 游戏化 --
POST   /api/students/{id}/growth/add    → 增加成长值（其他模块事件触发）
         body: { amount, source, source_id }  # source: "task_complete", "exam_pass", "streak"...

POST   /api/students/{id}/achievements  → 授予徽章（系统自动判定）

# -- 排行 --
GET    /api/leaderboard                 → 排行榜
         ?course_id={id}&type={progress|score|coins}
```

### 7.4 对外提供的数据查询方法

```python
def get_student_info(student_id: str) -> StudentInfo
    """所有模块都可能需要查询学生基本信息"""

def get_student_profile(student_id: str, course_id: str) -> StudentProfile
    """模块5 风险预警时需要查看 HP/DEF/进度"""

def get_students_by_class(class_id: str) -> list[StudentInfo]
    """模块5 班级维度分析时需要"""

def get_student_competency(student_id: str, course_id: str) -> list[CompetencyScore]
    """模块1 知识点掌握度展示时需要"""
```

### 7.5 依赖

| 依赖方 | 需要什么 | 说明 |
|--------|---------|------|
| 模块1 | KnowledgePointID, AbilityPointID | 能力评分必须关联能力点；推荐目标可能指向知识点 |
| 模块2 | LearningLog, Submission | 画像的 HP/ATK/DEF 计算依赖行为日志和提交记录 |
| 模块3 | ScoreRecord, MistakeRecord | 能力评分依赖考试成绩；推荐依赖错题分析 |
| 模块5 | ClassID | 学生归属班级 |
| agentic | 画像数据 + 知识点数据 | 个性化推荐需要 LLM |

---

## 八、模块5 详细接口：学情分析与教学决策

### 8.1 核心实体

| 实体 | 说明 | 关键字段 |
|------|------|---------|
| `Class` | 班级 | id, name, course_id, teacher_id, semester |
| `AnalyticsReport` | 分析报告 | id, class_id, report_type, data_json, generated_at |
| `RiskAlert` | 学习风险预警 | id, student_id, course_id, risk_type, risk_level, detail, created_at |
| `TeachingSuggestion` | 教学建议 | id, class_id, course_id, suggestion_type, content, generated_at |

### 8.2 对外暴露接口

```
模块5 对外暴露：
────────────────

# -- 班级信息 --
GET    /api/classes/{id}                → 班级信息
GET    /api/classes                     → 班级列表
         ?teacher_id={id}

# -- 预警查询（供模块4推送通知） --
GET    /api/classes/{id}/risk-alerts    → 班级风险学生列表
GET    /api/students/{id}/risk-status   → 某学生当前风险状态
```

### 8.3 自身完整接口

```
# -- 班级管理（管理员/教师） --
POST   /api/classes                     → 创建班级
PUT    /api/classes/{id}                → 修改班级信息
POST   /api/classes/{id}/enroll         → 添加学生到班级

# -- 成绩查看（教师端） --
GET    /api/classes/{id}/scores         → 班级成绩总览
GET    /api/classes/{id}/score-trends   → 成绩趋势图表数据
         ?student_id={id}&time_range={start,end}

# -- 进度查看（教师端） --
GET    /api/classes/{id}/progress       → 班级整体学习进度
GET    /api/classes/{id}/task-completion → 任务完成率详情

# -- 风险预警（→ agentic） --
POST   /api/classes/{id}/risk-detect    → 执行学习风险检测
         # 系统自动定时运行，也支持教师手动触发
         # 返回: RiskAlert[] 列表

# -- 干预建议（→ agentic） --
POST   /api/students/{id}/intervention  → 生成干预建议

# -- 反馈汇总 --
GET    /api/classes/{id}/feedback-summary
                                       → 学生提问与反馈汇总

# -- 共性问题聚类（→ agentic） --
POST   /api/classes/{id}/problem-cluster
                                       → 执行共性问题聚类分析
         # 返回: ClusterResult[] 列表

# -- 教学建议（→ agentic） --
POST   /api/classes/{id}/teaching-suggestions
                                       → 生成教学调整建议

# -- 报表导出 --
POST   /api/classes/{id}/reports/export  → 导出成绩报表（Excel/PDF）
```

### 8.4 对外提供的数据查询方法

```python
def get_class_risk_alerts(class_id: str) -> list[RiskAlert]
    """模块4 可以根据预警调整推荐策略"""

def get_student_risk_status(student_id: str) -> RiskStatus | None
    """前端展示学生风险状态"""
```

### 8.5 依赖

| 依赖方 | 需要什么 | 说明 |
|--------|---------|------|
| 模块1 | CourseID, KnowledgePointID | 分析报告需要课程和知识点维度 |
| 模块2 | Submission, LearningLog | 进度查看、风险预警的基础数据源 |
| 模块3 | ScoreRecord, MistakeStats | 成绩趋势、共性问题分析的数据源 |
| 模块4 | StudentInfo, StudentProfile | 学生画像数据用于预警判断 |
| agentic | 综合学习数据 | 预警判断、问题聚类、教学建议都需要 LLM |

---

## 九、agentic 服务（公共 AI 能力层）

> agentic 是独立服务，不是第6个业务模块。它只提供 LLM 推理能力，不持有业务数据表。

### 9.1 服务接口

```
POST   /api/agent/knowledge-extract     → 知识图谱自动提取
         body: { course_id, materials: [{type, content}] }
         returns: { knowledge_points: [{name, description, level, prerequisites[]}] }

POST   /api/agent/diagnostic-test       → 入学诊断评测
         body: { student_id, course_id, knowledge_map }
         returns: { recommended_start_kp, skipped_kp[], weak_kp[] }

POST   /api/agent/chat                  → AI 对话（知识点讲解/问答）
         body: { student_id, knowledge_point_id, context, question }
         returns: { answer, follow_up_questions[] }

POST   /api/agent/grade                 → 智能批改
         body: { submission_id, submission_type,  content, rubric }
         returns: { score, feedback, improvement_suggestions[] }

POST   /api/agent/recommend             → 个性化推荐
         body: { student_id, course_id, profile, recommendation_types[] }
         returns: { recommendations: [{type, target_id, reason, priority}] }

POST   /api/agent/risk-detect           → 学习风险检测
         body: { student_id, learning_data, thresholds }
         returns: { risk_level, risk_types[], detail }

POST   /api/agent/cluster-problems      → 共性问题聚类
         body: { class_id, course_id, mistakes[], feedbacks[], questions[] }
         returns: { clusters: [{topic, count, typical_cases[], severity}] }

POST   /api/agent/teaching-suggestions  → 教学建议生成
         body: { class_id, course_id, weak_points[], clusters[], progress_data }
         returns: { suggestions: [{content, target, urgency}] }
```

### 9.2 调用关系总览

```
模块1 ──→ agentic: 知识提取、PPT讲解、知识点问答
模块3 ──→ agentic: 智能批改
模块4 ──→ agentic: 个性化推荐
模块5 ──→ agentic: 风险检测、问题聚类、教学建议
模块2 ──→ agentic: （无直接调用，但日志数据是其他模块调用 agentic 的输入）
```

---

## 十、模块间数据流

```
                          ┌─────────────────────────┐
                          │      模块1：课程内容      │
                          │      与知识组织          │
                          │  Course · Resource      │
                          │  KnowledgePoint         │
                          │  KnowledgeRelation      │
                          │  AbilityPoint           │
                          └─────┬─────────┬─────────┘
                                │         │
              知识点ID          │         │  知识点ID、能力点ID
              知识图谱结构       │         │
              ┌─────────────────┘         └───────────────────┐
              │                                                │
              ▼                                                ▼
   ┌──────────────────────┐                      ┌──────────────────────┐
   │   模块2：学习任务     │                      │   模块3：测评与评价    │
   │   与过程跟踪          │                      │                      │
   │  Task · Submission   │  学生ID、提交内容     │  Question · Exam     │
   │  LearningLog         │ ──────────────────→  │  Answer · GradeResult│
   └──────────┬───────────┘                      └──────────┬───────────┘
              │                                             │
              │ 日志数据、提交记录                            │ 成绩数据、错题数据
              │                                             │
      ┌───────┴───────────────────────┐     ┌───────────────┘
      │                               │     │
      ▼                               ▼     ▼
   ┌──────────────────────┐    ┌──────────────────────┐
   │   模块4：学生画像     │    │   模块5：学情分析     │
   │   与个性化学习         │    │   与教学决策          │
   │                      │    │                      │
   │  Student · Profile   │    │  Class · Report      │
   │  CompetencyScore     │◄───│  RiskAlert           │
   │  Recommendation      │    │  TeachingSuggestion  │
   │  Achievement         │    │                      │
   └──────────────────────┘    └──────────────────────┘
              │                           │
              │ 画像数据                   │ 预警数据
              └───────────┬───────────────┘
                          │
                          ▼
                    前端展示层
```

**数据流向原则**：

1. **模块1 → 所有模块**：知识点ID是全局引用，模块1是唯一的知识点数据源
2. **模块2 → 模块3/4/5**：模块2是学习行为数据的**主要采集者**，但不生产测评数据
3. **模块3 → 模块4/5**：模块3是测评数据的**唯一源**，成绩和错题由模块3产出
4. **模块4 和 模块5 互相配合**：模块4 专注于单个学生的理解和适配；模块5 专注于班级维度的分析和决策
5. **所有模块 → agentic**：遇到 LLM 推理需求时，各模块自行调用 agentic，不通过其他模块中转

---

## 十一、开发阶段与依赖顺序

### 阶段划分

```
Phase 1 ────→ Phase 2 ────→ Phase 3 ────→ Phase 4
(基础)        (核心)        (智能)        (集成)
Week 1-2      Week 3-5      Week 6-8      Week 9-10
```

### Phase 1：地基（Week 1-2）

**目标**：所有模块能启动，能独立建表、写基本 CRUD，其他模块拿到 ID 即可 mock 并行开发。

| 谁 | 做什么 | 产出物 |
|----|--------|--------|
| **全员** | 项目脚手架：后端框架选型、数据库选型（金仓）、统一鉴权（JWT） | 可运行的空后端 + 登录注册 |
| **模块1** | Course CRUD、KnowledgePoint CRUD、定义知识点 JSON Schema | CourseID / KnowledgePointID 可用 |
| **模块2** | Task 基本 CRUD（发布/修改/删除/查看） | TaskID 可用 |
| **模块3** | Question 基本 CRUD（创建/修改/删除/按类型查询） | QuestionID 可用 |
| **模块4** | Student 基本 CRUD（录入/修改/查询） | StudentID 可用 |
| **模块5** | Class 基本 CRUD（创建班级/添加学生） | ClassID 可用 |

**Phase 1 完成标准（硬性检查点）**：
- [ ] 每人执行自己模块的建表 SQL，所有表在金仓数据库上创建成功
- [ ] 每人至少完成一个实体的一组增删改查并通过测试（Postman 或单元测试）
- [ ] 所有模块产出自己的 ID Schema 文档（共享给其他人）

### Phase 2：核心业务（Week 3-5）

**目标**：没有 AI 加持的核心业务功能跑通，各模块完成主体功能。

| 谁 | 做什么 | 需要等谁 |
|----|--------|---------|
| **模块1** | 资源上传/预览、知识点关系管理、知识图谱手动构建、知识点详情页 | Phase1 自身产出 |
| **模块2** | 任务提交/统计、学习日志采集、进度查询、视频日志记录 | Phase1 自身产出 + StudentID（模块4） |
| **模块3** | 试题关联知识点、组卷（随机/按知识点/按难度）、学生答题提交、自动判分 | Phase1 自身产出 + KnowledgePointID（模块1） |
| **模块4** | 学生画像基础计算（HP/ATK/DEF）、能力评分基础版、成长值/徽章 | LearningLog（模块2）、ScoreRecord（模块3）、CompetencyPoint（模块1） |
| **模块5** | 班级成绩查看、进度查看、成绩趋势图、成绩报表导出 | ScoreRecord（模块3）、Submission（模块2） |

**Phase 2 完成标准**：
- [ ] 核心业务流程能走通（教师发布任务 → 学生提交 → 自动判分 → 教师查看成绩）
- [ ] 学习日志能正确采集视频播放行为

### Phase 3：AI 智能（Week 6-8）

**目标**：接入 agentic 服务，所有 LLM 驱动的功能上线。**本阶段真正的依赖是 agentic 服务的就绪时间**。

| 谁 | 做什么 | 需要等谁 |
|----|--------|---------|
| **模块1** | 知识点自动提取、知识图谱自动构建、PPT讲解、知识点问答 | agentic `/extract`、`/chat` |
| **模块3** | 智能批改（报告/简答/论文）、修改建议生成 | agentic `/grade` |
| **模块4** | 个性化推荐（知识点/复习/拓展/练习）、能力评分动态更新 | agentic `/recommend` + Phase2 数据 |
| **模块5** | 学习风险预警、干预建议、共性问题聚类、教学建议 | agentic `/risk-detect`、`/cluster`、`/suggest` + Phase2 数据 |
| **模块2** | 本阶段无 agentic 调用，完善日志采集的多样性 | — |

**Phase 3 完成标准**：
- [ ] 每个 agentic 接口至少有一个功能落地并验证效果
- [ ] 智能批改的准确率经过人工抽检

### Phase 4：集成联调（Week 9-10）

**目标**：全链路测试、性能优化、文档补齐、答辩准备。

| 谁 | 做什么 |
|----|--------|
| **全员** | 全流程集成测试（教师端+学生端所有场景） |
| **模块2负责人** | 组织集成测试、CI/CD、项目打包 |
| **全员** | Bug 修复、前端打磨、答辩 PPT/演示准备 |

---

## 十二、协作规范

### 12.1 模块包结构约定

```
backend/
├── src/
│   ├── shared/                  # 公共层：鉴权、数据库连接、工具函数
│   │   ├── auth.py
│   │   ├── database.py
│   │   └── utils.py
│   ├── module1_course/          # 模块1
│   │   ├── models.py
│   │   ├── routes.py
│   │   ├── service.py
│   │   └── __init__.py          # 导出公共查询方法
│   ├── module2_task/            # 模块2
│   │   ├── models.py
│   │   ├── routes.py
│   │   ├── service.py
│   │   └── __init__.py
│   ├── module3_assessment/      # 模块3
│   │   ├── models.py
│   │   ├── routes.py
│   │   ├── service.py
│   │   └── __init__.py
│   ├── module4_student/         # 模块4
│   │   ├── models.py
│   │   ├── routes.py
│   │   ├── service.py
│   │   └── __init__.py
│   ├── module5_analytics/       # 模块5
│   │   ├── models.py
│   │   ├── routes.py
│   │   ├── service.py
│   │   └── __init__.py
│   └── main.py                  # FastAPI/Flask 入口，注册所有路由
```

### 12.2 接口契约与 Mock

**原则**：当你开发需要调用其他模块的数据时，先用 mock 数据跑通自己模块的逻辑，不等别人开发完。

```python
# 示例：模块4 需要调用模块2的日志查询
# 在 module4_student/service.py 中：

# 正式调用：
from module2_task import get_student_logs as _get_logs

# Phase 1-2 期间，如果模块2还没提供，先用 mock：
def get_student_logs(student_id, course_id, action_types=None, time_range=None):
    try:
        from module2_task import get_student_logs as _get_logs
        return _get_logs(student_id, course_id, action_types, time_range)
    except (ImportError, NotImplementedError):
        # Mock 数据，等模块2就绪后自动切换
        return [
            {"action_type": "code_submit", "duration_ms": 300000, "timestamp": "..."},
            {"action_type": "video_play", "duration_ms": 1200000, "timestamp": "..."},
        ]
```

### 12.3 数据库表命名约定

```
表名格式：{模块前缀}_{实体名}

模块1:  course_, resource_, knowledge_point_, knowledge_relation_, ability_point_
模块2:  task_, submission_, learning_log_
模块3:  question_, exam_, answer_, grade_result_
模块4:  student_, profile_, competency_score_, recommendation_, achievement_
模块5:  class_, analytics_report_, risk_alert_, teaching_suggestion_
```

### 12.4 API 路由命名约定

```
模块1: /api/courses/*              /api/knowledge-points/*    /api/resources/*
        /api/knowledge-relations/*  /api/ability-map/*

模块2: /api/tasks/*                /api/submissions/*         /api/learning-logs/*
        /api/students/{id}/progress

模块3: /api/questions/*            /api/exams/*               /api/students/{id}/scores
        /api/students/{id}/mistakes

模块4: /api/students/*             /api/leaderboard/*

模块5: /api/classes/*              /api/classes/{id}/scores   /api/classes/{id}/risk-alerts
```

### 12.5 Git 分支策略

```
master（主分支，只合并稳定功能）
  ├── feature/module1-knowledge-graph    # 模块1 知识图谱
  ├── feature/module1-resource           # 模块1 资源管理
  ├── feature/module2-task-pipeline      # 模块2 任务管道
  ├── feature/module3-question-bank      # 模块3 题库
  ├── feature/module4-student-profile    # 模块4 画像
  ├── feature/module5-analytics          # 模块5 分析
  └── ...

规则：
  - 每人从 master 拉分支开发自己的模块功能
  - 每个可验证的小增量提交（至少每天一次）
  - 合并前确保自己的分支与 master 无冲突
  - 合并到 master 后立即在群里通知其他人 git pull
```

### 12.6 接口变更流程

当你需要修改自己模块对外暴露的接口时（增删字段、修改返回格式）：

1. **先更新本文档** 对应章节
2. 在群内通知所有人变更内容
3. 确保自己的代码兼容旧接口至少一个 Phase（标注 `@deprecated`）
4. 依赖方确认迁移完毕后删除旧接口

---

## 十三、每个人的启动检查清单

在 Phase 1 开始时，确认：

- [ ] 金仓数据库环境已搭建，能成功连接
- [ ] 后端项目脚手架已创建（语言/框架选定），能启动 Hello World
- [ ] 已阅读本文档中自己模块的接口清单
- [ ] 已建好自己模块的数据表（至少核心实体的建表 SQL）
- [ ] 已告知其他人自己模块产出的 ID 字段格式
- [ ] 与其他人的依赖关系已确认，知道谁需要等谁

---

> 📁 本文档属于 `docs/architecture/` — 上承 `specs/项目需求.md`（应该建什么），下接各模块代码实现。
>
> ⚠️ 本文档是**活的契约**——任何人均可提出修改，但修改前必须在群内征求依赖方同意。
