spring:
  application:
    name: practical-training-backend
  sql:
    init:
      mode: never
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://6hvKfTDx9562cF6.root:VOPrnl0gCDYLjYhD@gateway01.ap-southeast-1.prod.aws.tidbcloud.com:4000/practical_training?useSSL=true&requireSSL=true&serverTimezone=UTC&connectTimeout=30000&socketTimeout=60000
    hikari:
      maximum-pool-size: 20
      minimum-idle: 3
      idle-timeout: 600000
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 8080
  servlet:
    context-path: /practical-training

logging:
  level:
    "[com.neu.CoursePlatform.mapper]": debug
    "[com.neu.CoursePlatform.controller]": debug

mybatis-plus:
  type-aliases-package: com.neu.CoursePlatform.entity
  mapper-locations: classpath*:/mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
