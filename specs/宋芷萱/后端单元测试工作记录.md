# 后端单元测试工作记录

> 负责人：宋芷萱 | 日期：2026-06-29 | 范围：后端 Service 层 + AI 模块单元测试（全部模块）

---

## 一、做了什么

### 第一阶段：业务模块测试（10 个新增，90 个用例）

| 文件 | 模块 | 用例数 | 测试对象 |
|------|------|--------|---------|
| `TeacherServiceTest.java` | 认证 | 9 | 教师登录/注册/搜索 |
| `StudentServiceTest.java` | 认证 | 14 | 学生登录/注册/搜索/CSV导入导出/班级查询 |
| `CourseServiceTest.java` | 模块1 | 6 | 课程搜索/DTO转换/课时列表 |
| `LessonServiceTest.java` | 模块1 | 9 | 课时CRUD/搜索 |
| `StatsServiceTest.java` | 模块2 | 5 | 学生成绩统计/课程任务统计 |
| `LearningProgressServiceTest.java` | 模块2 | 7 | 学生进度/课程进度/滞后检测 |
| `QuestionServiceTest.java` | 模块3 | 16 | 8种组卷策略/异常校验 |
| `ExamServiceTest.java` | 模块3 | 6 | 试卷生成保存/绑定发布 |
| `AbilityPointServiceTest.java` | 模块4 | 3 | 能力点查询 |
| `RiskAlertServiceTest.java` | 模块5 | 8 | 预警创建/去重/解决 |
| `ClassInfoServiceTest.java` | 模块5 | 7 | 班级CRUD/学生管理 |

### 第二阶段：AI 模块测试（7 个新增，118 个用例）

| 文件 | 子模块 | 用例数 | 测试对象 |
|------|--------|--------|---------|
| `DifyResponseTest.java` | Dify | 16 | 响应封装：success/error 工厂方法、属性读写、独立性 |
| `DifyClientTest.java` | Dify | 20 | API 客户端：isConfigured、API Key 缺失错误、HTTP 不可达降级 |
| `DifyKnowledgeServiceTest.java` | Dify | 14 | 知识库检索、退避机制、JSON 解析容错、RAG Prompt 构建 |
| `AgenticRequestTest.java` | Agentic | 5 | 请求 DTO：字段 getter/setter、默认值 |
| `AgenticResponseTest.java` | Agentic | 7 | 响应 DTO：构造函数、unavailable() 工厂方法 |
| `AgenticClientTest.java` | Agentic | 32 | 多模式路由（mock/dify/deepseek/http）、10 种 capability、异常处理 |
| `CourseAiServiceImplTest.java` | Service | 24 | 知识点讲解/问答/能力图谱生成、参数校验、AI 不可达降级 |

### 项目已有测试文件（2 个，16 个用例）

| 文件 | 用例数 | 来源 |
|------|--------|------|
| `Module2ServiceTest.java` | 8 | 项目自带（模块2核心逻辑） |
| `ScoreAnalysisServiceImplTest.java` | 1 | 项目自带（模块5空班统计） |

### 总计：**19 个测试文件，216 个测试用例，0 失败，0 错误**

---

## 二、修改的项目文件

### pom.xml — 新增 JaCoCo 插件

```xml
<plugin>
    <groupId>org.jacoco</groupId>
    <artifactId>jacoco-maven-plugin</artifactId>
    <version>0.8.12</version>
</plugin>
```

### pom.xml — 修复 argLine 冲突

原来的 surefire 插件 argLine 写死了 byte-buddy agent 路径，会导致 JaCoCo agent 无法加载。改为：

```xml
<argLine>${argLine} -javaagent:${settings.localRepository}/net/bytebuddy/byte-buddy-agent/1.17.8/byte-buddy-agent-1.17.8.jar</argLine>
```

在 `-javaagent` 前加了 `${argLine}` 占位符，让 JaCoCo 能通过 `prepare-agent` 注入自己的 agent。

### 其他修改

| 文件 | 改动 | 原因 |
|------|------|------|
| `CourseDetail.vue` | 任务截止时间：文本框 → 日历选择器 | 模块2需求 |
| `TaskList.vue` | 同上 | 模块2需求 |

---

## 三、测试技术方案

### 为什么不直接用 Mockito

项目已有的 `ProfileServiceImplTest` 用了 Mockito，但 Mockito 依赖 byte-buddy，和项目 surefire 配置的 byte-buddy-agent 版本有冲突，在 JDK 23 下运行时报 `MockMaker` 加载失败。

### 方案一：动态代理（用于接口类 Mapper/Service）

参考项目自带的 `Module2ServiceTest.java` 的写法，用 `java.lang.reflect.Proxy` 动态生成接口的模拟实现：

```java
TeacherMapper proxy = (TeacherMapper) Proxy.newProxyInstance(
    TeacherMapper.class.getClassLoader(),
    new Class<?>[]{TeacherMapper.class},
    (p, method, args) -> {
        switch (method.getName()) {
            case "selectByUsername": return store.findByUsername((String) args[0]);
            case "insert": store.put(...); return 1;
        }
    });

// 注入到 ServiceImpl 的 baseMapper 字段（递归查找父类）
setBaseMapper(service, proxy);
```

### 方案二：匿名子类（用于具体类 Component/Service）

AI 模块的 `DifyClient`、`AgenticClient`、`DifyKnowledgeService` 是具体类（`@Component`、`@Service`），不能用 `Proxy`。改用匿名子类覆写方法：

```java
DifyClient mockClient = new DifyClient() {
    @Override
    public boolean isConfigured() { return true; }

    @Override
    public DifyResponse sendChatMessage(String query, String userId) {
        return DifyResponse.success("Mock response: " + query);
    }
};
```

### 注入 baseMapper 的坑

MyBatis-Plus 的 `baseMapper` 字段不在 `ServiceImpl` 中，而是在父类 `BaseServiceImpl` 里，普通反射 `getDeclaredField` 找不到。需要递归遍历类层级：

```java
static void setBaseMapper(Object service, Object mapper) throws Exception {
    Class<?> clazz = service.getClass();
    while (clazz != null) {
        try {
            Field f = clazz.getDeclaredField("baseMapper");
            f.setAccessible(true);
            f.set(service, mapper);
            return;
        } catch (NoSuchFieldException e) {
            clazz = clazz.getSuperclass();
        }
    }
}
```

---

## 四、运行方式

```bash
cd backend

# 加载环境变量
set -a && source .env && set +a

# 运行全部单元测试（19个文件）
mvn test -Dtest="TeacherServiceTest,StudentServiceTest,LessonServiceTest,QuestionServiceTest,Module2ServiceTest,CourseServiceTest,ExamServiceTest,StatsServiceTest,LearningProgressServiceTest,RiskAlertServiceTest,ClassInfoServiceTest,AbilityPointServiceTest,ProfileServiceImplTest,ScoreAnalysisServiceImplTest,DifyResponseTest,DifyClientTest,DifyKnowledgeServiceTest,AgenticRequestTest,AgenticResponseTest,AgenticClientTest,CourseAiServiceImplTest" -DfailIfNoTests=false

# 运行测试 + 生成 JaCoCo 覆盖率报告
mvn test jacoco:report -Dtest="..."

# 查看覆盖率报告
# 用浏览器打开：backend/target/site/jacoco/index.html
```

---

## 五、覆盖率情况

### 业务模块 Service 层（12 个）

| 被测类 | 行覆盖率 | 方法覆盖率 | 分支覆盖率 |
|--------|---------|-----------|-----------|
| TeacherServiceImpl | 100% (9/9) | 100% (4/4) | 100% (6/6) |
| StudentServiceImpl | 92% (49/53) | 100% (7/7) | 91% (20/22) |
| CourseServiceImpl | 100% (24/24) | 100% (7/7) | 100% (2/2) |
| ExamServiceImpl | 100% (39/39) | 100% (5/5) | 71% (12/17) |
| QuestionServiceImpl | 96% (139/145) | 92% (36/39) | 68% (94/138) |
| StatsServiceImpl | 100% (116/116) | 89% (8/9) | 63% (49/78) |
| LearningProgressServiceImpl | 95% (143/150) | 89% (8/9) | 65% (55/84) |
| AbilityPointServiceImpl | 100% (2/2) | 100% (2/2) | — |
| RiskAlertServiceImpl | 92% (33/36) | 88% (7/8) | 69% (11/16) |
| ClassInfoServiceImpl | 76% (26/34) | 89% (8/9) | 64% (9/14) |
| LearningTaskServiceImpl | 50% (11/22) | 43% (3/7) | 58% (7/12) |
| LessonServiceImpl | 26% (5/19) | 75% (3/4) | 0% (0/4) |

### AI 模块（7 个类）

| 被测类 | 指令覆盖率 | 分支覆盖率 | 行覆盖率 | 说明 |
|--------|-----------|-----------|---------|------|
| CourseAiServiceImpl | 100% | 95% | 100% (50/50) | 核心业务逻辑完全覆盖 |
| DifyKnowledgeService | 100% | 95% | 100% (49/49) | 知识库检索与 RAG 完全覆盖 |
| DifyResponse | 100% | n/a | 100% (15/15) | 响应封装完全覆盖 |
| KnowledgeSegment | 100% | n/a | 100% (7/7) | POJO 完全覆盖 |
| AgenticResponse | 100% | n/a | 100% (1/1) | DTO 完全覆盖 |
| AgenticException | 100% | n/a | 100% (2/2) | 异常类完全覆盖 |
| DifyClient | 70% | 58% | 77/148 行 | HTTP 调用路径依赖外部服务 |
| AgenticClient | 53% | 41% | 113/209 行 | DeepSeek/HTTP 模式依赖外部 API |

> **说明**：`DifyClient` 和 `AgenticClient` 的部分代码（真实 HTTP 请求、外部 API 响应解析）需要实际运行的外部服务才能触发，所有可脱离外部依赖测试的逻辑（配置检查、模式路由、参数校验、错误降级、Mock 模式）均已 100% 覆盖。

### 未覆盖的部分

- **Controller 层**（30+个）：强依赖 HTTP Session 和 Spring 上下文，需要 `@SpringBootTest + MockMvc` 集成测试
- **复杂 Service**（ProfileServiceImpl、TaskSubmissionServiceImpl、SubmissionAiReviewServiceImpl 等）：依赖过多（5~10个 Mapper/Service），纯单元测试意义不大
- **DifyClient/AgenticClient HTTP 路径**：依赖外部 LLM 服务
- **文件存储服务**：依赖文件系统

---

## 六、遇到的问题和解决

| 问题 | 解决方案 |
|------|---------|
| Mockito 初始化失败（byte-buddy 版本冲突） | 改用 Java 动态代理 + 匿名子类，与项目现有测试风格一致 |
| `AgenticClient` 是具体类，`Proxy` 不能 mock | 改用匿名子类 `new AgenticClient(...) { @Override ... }` |
| `knowledgeExtract/chat/grade/recommend/riskDetect` 无 mock 模式分支 | 改为 dify 模式测试，用 mock DifyClient 返回成功响应 |
| `baseMapper` 字段反射不到 | 递归遍历 ServiceImpl 的父类层级 |
| JaCoCo agent 不生效（argLine 被覆盖） | 在 surefire argLine 中加 `${argLine}` 占位符 |
| JaCoCo 报告路径 C 盘 vs D 盘 | 项目的 WebConfig 硬编码了 C 盘路径，与当前 D 盘不符 |
| `var` 关键字导致编译错误 | 部分场景下 `var` 类型推断不兼容，显式写 `Class<?>` |
| Long vs Integer 断言失败 | `Math.round()` 返回 `long`，断言需要用 `assertEquals(L, ...)` |
| JDK 23 + 旧版 Mockito 不兼容 | 业务测试不用 Mockito，AI 模块测试用匿名子类替代 |

---

*文档位置：specs/宋芷萱/后端单元测试工作记录.md*
